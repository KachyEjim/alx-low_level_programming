#include <stdio.h>

/**
 * main - Entry point
 *
 * @argc: number of arguments
 * @argv: pointer to the array name of arguments
 *
 * Return: Always returns 0;
 *
 */

int main(int argc, char *argv[])
{
	(void)argc;

	printf("%s\n", argv[0]);
	return (0);
}
